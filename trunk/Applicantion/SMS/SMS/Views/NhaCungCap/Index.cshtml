@model SMS.Models.KHU_VUC

<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
<link href="~/Content/css/style.css" rel="stylesheet" type="text/css" />

@{
    ViewBag.Title = "Danh mục nhà cung cấp";
}

<h2>@ViewBag.Title </h2>

<style type="text/css">

</style>
<hr style="width:100%" />
<div style="float:right">

    @using (Html.BeginForm("Index", "NhaCungCap", FormMethod.Post))
    {
        <p>
            Từ khóa tìm kiếm: @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)
            <input type="submit" value="Tìm kiếm" class="myButton" />
            @{
        if ((bool)Session["IsAdmin"] == true || (bool)Session["IsMetadataManager"] == true)
        {
            <input type="button" value="Thêm Mới" class="myButton" onclick="window.location.href('@Url.Action("AddNew","NhaCungCap")')" />
        }
            }
        </p>
    }


</div>
<hr style="width:100%" />
<table class="webGrid">

    <tr>
        <th title="Mã nhà cung cấp">Mã NCC</th>
        <th title="Tên Nhà cung cấp">Tên nhà cung cấp</th>
        <th title="Số điện thoại">Số điện thoại</th>
        @{
            if ((bool)Session["IsAdmin"] == true)
            {
                <th>Người tạo</th>
                <th>Ngày tạo</th>
                <th title=" Người Cập Nhật">Người CN</th>
                <th title=" Ngày Cập Nhật">Ngày CN</th>
            }
        }
        @{
            if ((bool)Session["IsAdmin"] == true || (bool)Session["IsMetadataManager"] == true)
            {
                <th style="text-align:center">Sửa</th>
                <th style="text-align:center">Xóa</th>
            }
        }
    </tr>
    @foreach (var donvi in ViewBag.theList)
    {
        <tr>
            <td> @donvi.NhaCungCap.MA_NHA_CUNG_CAP </td>
            <td> @donvi.NhaCungCap.TEN_NHA_CUNG_CAP</td>
            <td> @donvi.NhaCungCap.SO_DIEN_THOAI </td>
            @{
        if ((bool)Session["IsAdmin"] == true)
        {
            <td> @donvi.NguoiTao.TEN_NGUOI_DUNG </td>
                <td> @donvi.NhaCungCap.CREATE_AT.ToString("dd/MM/yyyy")  </td>
                <td> @donvi.NguoiCapNhat.TEN_NGUOI_DUNG </td>
                <td> @donvi.NhaCungCap.UPDATE_AT.ToString("dd/MM/yyyy")  </td>
        }
            }

            @{
        if ((bool)Session["IsAdmin"] == true || (bool)Session["IsMetadataManager"] == true)
        {
            <td>
                <a href="@Url.Action("Edit", "NhaCungCap", new { id = @donvi.NhaCungCap.MA_NHA_CUNG_CAP })" class="myEditButton">
                    <img width="24" height="24" alt="edit" src="~/Content/images/edit-icon.png"></img>
                </a>
            </td>
                <td>
                    <a href="@Url.Action("Delete", "NhaCungCap", new { id = @donvi.NhaCungCap.MA_NHA_CUNG_CAP })" class="myEditButton" onclick="return deleteSubmit()">
                        <img width="24" height="24" alt="edit" src="~/Content/images/edit_delete.png"></img>
                    </a>
                </td>
        }
            }


        </tr>
    }

</table>
<input type="hidden" value="" id="delete" />
<script>
    function deleteSubmit() {
        var r = confirm("Bạn muốn xóa nhà cung cấp này!");
        if (r == true) {
            document.forms[0].submit();
        }
        else {
            return false;
        }
    }
</script>   
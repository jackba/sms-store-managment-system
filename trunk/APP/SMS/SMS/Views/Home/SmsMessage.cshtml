@model SMS.Models.SMS_MESSAGES


    @if (@Model != null)
    {
        <h3 style="color:red">
            Thông báo từ @Model.NGUOI_DUNG1.TEN_NGUOI_DUNG
        </h3>
    <div style="background-color:lightblue; font-family:'Times New Roman'; font-size:20px;">
        @Model.NOI_DUNG
    </div>
    }
